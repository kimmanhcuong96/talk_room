import { Check, ChevronLeft, LoaderCircle, X } from "lucide-react";
import { useCallback, useEffect, useState } from "react";
import { getVerificationRequests, reviewVerificationRequest, reviewVerificationRequests, type AdminVerificationRequest } from "../../lib/adminAuth";
import type { Language } from "../../lib/i18n";
import { adminPath } from "../../lib/routes";
import type { AdminTranslationKey } from "../../lib/adminI18n";

type Props = { token: string; language: Language; t: (key: AdminTranslationKey) => string };

export function VerificationRequestsPageBulk({ token, t }: Props) {
  const [requests, setRequests] = useState<AdminVerificationRequest[]>([]);
  const [selectedIds, setSelectedIds] = useState<Set<string>>(() => new Set());
  const [loading, setLoading] = useState(true);
  const [reviewing, setReviewing] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const load = useCallback(async () => { setLoading(true); setError(null); try { setRequests(await getVerificationRequests(token)); setSelectedIds(new Set()); } catch (loadError) { setError(loadError instanceof Error ? loadError.message : "REQUEST_FAILED"); } finally { setLoading(false); } }, [token]);
  useEffect(() => { void load(); }, [load]);
  const toggle = (id: string) => setSelectedIds((current) => { const next = new Set(current); if (next.has(id)) next.delete(id); else next.add(id); return next; });
  const allSelected = requests.length > 0 && requests.every((request) => selectedIds.has(request.id));
  const review = async (ids: string[], decision: "approved" | "rejected") => { if (!ids.length) return; setReviewing(true); setError(null); try { if (ids.length === 1) await reviewVerificationRequest(token, ids[0], decision); else await reviewVerificationRequests(token, ids, decision); setRequests((current) => current.filter((request) => !ids.includes(request.id))); setSelectedIds(new Set()); } catch (reviewError) { setError(reviewError instanceof Error ? reviewError.message : "REQUEST_FAILED"); } finally { setReviewing(false); } };
  return <div className="grid gap-5"><a href={adminPath()} className="inline-flex w-fit items-center gap-2 text-sm text-white/60 hover:text-white"><ChevronLeft size={16}/>{t("backToDashboard")}</a><div><h2 className="text-2xl font-semibold">{t("verificationRequests")}</h2><p className="mt-2 text-sm text-white/60">{t("verificationRequestsDescription")}</p></div>{error ? <p className="rounded-md border border-coral/30 bg-coral/10 px-4 py-3 text-sm text-coral">{error}</p> : null}{loading ? <p className="py-10 text-center text-white/50"><LoaderCircle className="mr-2 inline animate-spin" size={17}/>{t("loading")}</p> : requests.length === 0 ? <p className="rounded-lg border border-white/10 bg-panel px-4 py-10 text-center text-white/50">{t("noVerificationRequests")}</p> : <><div className="flex flex-wrap items-center justify-between gap-3 rounded-lg border border-white/10 bg-panel p-3"><label className="flex cursor-pointer items-center gap-2 text-sm text-white/75"><input type="checkbox" checked={allSelected} onChange={() => setSelectedIds(allSelected ? new Set() : new Set(requests.map((request) => request.id)))} className="h-4 w-4 cursor-pointer accent-[#75f2b8]"/><span>{selectedIds.size}/{requests.length}</span></label><div className="flex gap-2"><button type="button" disabled={!selectedIds.size || reviewing} onClick={() => void review([...selectedIds], "rejected")} className="inline-flex h-9 items-center gap-2 rounded-md bg-coral/15 px-3 text-xs font-semibold text-coral disabled:cursor-not-allowed disabled:opacity-40 hover:bg-coral/25"><X size={15}/>{t("reject")}</button><button type="button" disabled={!selectedIds.size || reviewing} onClick={() => void review([...selectedIds], "approved")} className="inline-flex h-9 items-center gap-2 rounded-md bg-mint px-3 text-xs font-semibold text-ink disabled:cursor-not-allowed disabled:opacity-40 hover:bg-mint/90"><Check size={15}/>{t("approve")}</button></div></div><div className="grid gap-4">{requests.map((request) => <article key={request.id} className={`rounded-lg border bg-panel p-5 ${selectedIds.has(request.id) ? "border-mint/45" : "border-white/10"}`}><div className="flex flex-wrap items-start gap-3"><label className="cursor-pointer pt-1"><input type="checkbox" checked={selectedIds.has(request.id)} onChange={() => toggle(request.id)} aria-label={request.displayName} className="h-4 w-4 cursor-pointer accent-[#75f2b8]"/></label><div className="min-w-0 flex-1"><h3 className="font-semibold">{request.displayName}</h3><p className="text-xs text-white/45">{request.email} · {new Date(request.createdAt).toLocaleString()}</p></div><span className="rounded-full bg-amber-300/15 px-2.5 py-1 text-xs text-amber-200">{request.status}</span></div><div className="mt-4 grid gap-2"><p className="text-xs uppercase tracking-wide text-white/45">{t("verificationMessage")}</p><p className="whitespace-pre-wrap rounded-md bg-white/[0.03] p-3 text-sm leading-6 text-white/80">{request.message}</p><p className="text-xs text-mint">{t("commitment")}: {request.communityCommitment ? "✓" : "—"}</p></div><div className="mt-4 flex justify-end gap-2"><button type="button" disabled={reviewing} onClick={() => void review([request.id], "rejected")} className="inline-flex h-9 items-center gap-2 rounded-md bg-coral/15 px-3 text-xs font-semibold text-coral disabled:opacity-40 hover:bg-coral/25"><X size={15}/>{t("reject")}</button><button type="button" disabled={reviewing} onClick={() => void review([request.id], "approved")} className="inline-flex h-9 items-center gap-2 rounded-md bg-mint px-3 text-xs font-semibold text-ink disabled:opacity-40 hover:bg-mint/90"><Check size={15}/>{t("approve")}</button></div></article>)}</div></>}</div>;
}
